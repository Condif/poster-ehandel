import React from "react";

const Header = () => {
  return (
    <img
      height="120"
      style={{ marginTop: "9px" }}
      src={require("../assets/logo.png")}
      alt="Logo"
    />
  );
};

export default Header;
