import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  labelText: {
    fontSize: "1.2rem",
    padding: "1rem",
    color: "black",
  },
  containerDiv: {
    margin: "1rem",
  },
}));

export default useStyles;
