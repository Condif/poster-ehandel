import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  labelText: {
    fontSize: "1.2rem",
    padding: "1rem",
    color: "black",
    marginTop: "1rem",
  },
  containerDiv: {
    margin: "1rem",
  },
}));

export default useStyles;
